import 'package:flutter/material.dart';

class CashTab extends StatelessWidget {
  const CashTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Cash'));
  }
}
